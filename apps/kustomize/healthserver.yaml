apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: healthserver
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/yadon-infra/manifest
    targetRevision: main
    path: application/healthserver
  destination:
    server: https://kubernetes.default.svc
    namespace: healthserver
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
